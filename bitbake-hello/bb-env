which bitbake &>/dev/null || \
	export PATH=~/.local/bitbake/bin:$PATH

export BBPATH="$(realpath $(dirname $(which bitbake))/..):$PWD"
alias bb_clean='rm -rf tmp/ meta-hl-layer/tmp/ bitbake-cookerdaemon.log *.env'

